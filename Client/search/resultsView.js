// Code for the search view
var resultsView = function(socket, username, templates, onLoadSearch)
{
	var element = $(templates.results())



	var loadResults = function(results)
	{
		

		return element;
	};

	return loadResults;
}